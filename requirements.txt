# Temel Django ve Web Sunucusu
Django==5.2.7
gunicorn
whitenoise==6.11.0
dj-database-url==3.0.1
asgiref==3.10.0
sqlparse==0.5.3

# Veri İşleme ve Yardımcılar
pandas==2.2.3
numpy==2.3.3 # pandas için gerekli
openpyxl==3.1.5 # Yeni .xlsx dosyaları için
et_xmlfile==2.0.0 # openpyxl için gerekli
python-dateutil==2.9.0.post0
pytz==2025.2
tzdata==2025.2 # pytz için gerekli

# Resim İşleme (OCR ve Diğerleri)
Pillow==10.4.0
pyzbar==0.1.9 # Barkod okuma için (varsa)
# pytesseract==0.3.13 # Tesseract OCR kullanıyorsanız ekleyin, Gemini varken gereksiz olabilir

# Google Gemini (OCR için)
google-generativeai
google-api-core>=1.31.5 # Gemini'nin bağımlılığı, pip otomatik halleder ama eklemek iyi olabilir
google-auth>=2.15.0 # Gemini'nin bağımlılığı
googleapis-common-protos>=1.56.0 # Gemini'nin bağımlılığı
proto-plus>=1.22.0 # Gemini'nin bağımlılığı
protobuf>=3.19.0 # Gemini'nin bağımlılığı

# İstekler ve Ağ
requests==2.32.5
aiohttp==3.13.0 # Llama'dan kalmış olabilir, async kullanmıyorsanız kaldırılabilir
aiohappyeyeballs==2.6.1 # aiohttp bağımlılığı
aiosignal==1.4.0 # aiohttp bağımlılığı
attrs==25.4.0 # aiohttp bağımlılığı
frozenlist==1.8.0 # aiohttp bağımlılığı
multidict==6.7.0 # aiohttp bağımlılığı
yarl==1.22.0 # aiohttp bağımlılığı
charset-normalizer==3.4.3 # requests bağımlılığı
idna==3.10 # requests bağımlılığı
urllib3==2.5.0 # requests bağımlılığı
certifi==2025.10.5 # requests bağımlılığı

# Diğerleri (Emin değilseniz kalsın)
click==8.3.0 # Genellikle komut satırı araçları için
colorama==0.4.6 # Renkli terminal çıktıları için
python-dotenv==1.1.1 # .env dosyalarını okumak için (Render Environment kullanıldığı için gereksiz olabilir)

# --- KALDIRILANLAR ---
# LlamaIndex ve ilişkili AI paketleri (Çakışma yaratıyordu)
# Streamlit, Altair, Matplotlib, Seaborn, Plotly, Pydeck (Web arayüzünde kullanılmıyorsa gereksiz)
# Scikit-learn, NLTK (Makine öğrenmesi/NLP kullanılmıyorsa gereksiz)
# SQLAlchemy, Greenlet (Django ORM varken gereksiz)
# Banks, Griffe, etc. (Ne işe yaradığı belirsiz veya gereksiz görünenler)